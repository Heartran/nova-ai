# Copia questo file in `.env` e inserisci il token del bot Nova.
# E' lo stesso token che sta in nova-ai/.env (DISCORD_TOKEN).
# Requisiti del bot: deve stare nel server dove sei tu e avere
# PRESENCE INTENT + SERVER MEMBERS INTENT attivi (gia' fatto sul Dev Portal).
#
# IMPORTANTE: niente spazi attorno all'=, niente virgolette, niente CR finale.
BOT_TOKEN=

# REDIS_HOST e' gia' impostato dal docker-compose (= redis); non serve qui.
